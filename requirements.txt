# Exact-pinned per CLAUDE.md §3 ("Unpinned dependencies... upgrading a
# pinned dependency without saying why" is forbidden). These are the direct
# dependencies; requirements.lock is the full resolved closure (pip freeze)
# for exact reproducibility. Frozen against the Python version recorded in
# PROGRESS.md.

# Core Agent Framework
langchain==1.3.15
langchain-community==0.4.2
langchain-core==1.5.4

# Data Validation & Serialization
pydantic==2.13.4
pyyaml==6.0.3

# Canonicalization (firewall/canonicalize.py — IDNA/punycode host encoding)
idna==3.18

# Policy engine (firewall/policy_engine.py) — regex with a real per-call
# timeout parameter, which the stdlib `re` module does not support and
# INV-09 (ReDoS protection) requires.
regex==2026.8.31

# Web API & Dashboard
fastapi==0.141.1
uvicorn==0.52.2
streamlit==1.61.1

# Testing & Utilities
pytest==9.1.1
python-dotenv==1.2.2
matplotlib==3.11.1
