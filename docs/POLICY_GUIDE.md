# Policy Authoring Guide

> Status: not yet written. `policies/` has no rule files yet (see `LIMITATIONS.md`).

Will document: the YAML policy schema, how `default_action` and per-tool
rules compose, canonicalization order (INV-06), and worked examples once
`firewall/policy_engine` exists.
