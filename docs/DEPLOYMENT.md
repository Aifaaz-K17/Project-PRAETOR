# Deployment

> Status: not yet written.

Will cover: how to run the demo end-to-end from a clean clone, environment
variables (`.env.example`), and the boundary of what this project claims to
be deployable as (a demo/research middleware, not a production control).
